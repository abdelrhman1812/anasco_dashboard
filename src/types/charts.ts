export interface TrafficByDeviceDataI {
    device: string;
    traffic: number;
    color: string;
}

export interface TrafficByLocationDataI {
    name: string;
    value: number;
    color: string;
}
